server:
  port: 8282

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/salary
    username: postgres
    password: Raja@nagar08
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  application:
    name: SALARY-SERVICE

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rajdeep-nagar08/payroll-microservice-config
          clone-on-start: true
#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka
      


